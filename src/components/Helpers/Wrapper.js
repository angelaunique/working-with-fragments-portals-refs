const Wrapper = (props) => {
  return props.children; // children prop holds all the content that passing bw the openning and closing tag of the custom component
};
export default Wrapper;
//basically an empty compoent
